# mosaic-RSU
we followed these steps mosaic\scenarios\HelloWorld\mapping
In the mapping_config we run the scenario with  0%, 50%, 100% CAM messages, and RSU in which we defined AdvisoryCAR (In this Scenario vehicles receive speed control limit).
we run these scenarios with this command mosaic.bat -s HelloWorld -v -b 1 and see the result.
In scenario_config we just changed The Duration of simulation from 120s to 200 s
